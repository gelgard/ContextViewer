# AI Task 080 — Stage 8 Production UI: Shared Shell + Design Tokens (Preview/Bootstrap)

## Stage
Stage 8 — Polish

## Substage
Post-Figma production UI implementation

## Goal
Перевести текущий Stage 8 bootstrap preview из transitional handoff-view в первый production-aligned UI slice: применить approved shell, navigation framing, typography/color/spacing tokens и общую visual hierarchy к существующим preview surfaces без изменения JSON/runtime semantics.

## Why This Matters
После design-sync branch (`062–079`) у проекта уже есть approved design authority. Следующий шаг — не генерировать новый дизайн, а начать поэтапно переносить утверждённый UI в реальные preview/product surfaces. Этот slice должен создать общий visual foundation для задач `081–083`.

## Goal Alignment
Requirement IDs (from `docs/plans/product_goal_traceability_matrix.md`):
- `PG-UX-001`
- `PG-EX-001`
- `PG-PL-001`

## Files to Create / Update
Update:
- `code/ui/render_ui_bootstrap_preview.sh`
- `code/ui/prepare_ui_preview_launch.sh`
- `code/ui/verify_stage8_ui_preview_delivery.sh`
- `code/ui/get_stage8_ui_preview_readiness_report.sh`
- `code/data_layer/README.md`

Optional update only if needed to preserve smoke/readiness correctness:
- `code/ui/start_ui_preview_server.sh`
- `code/ui/get_stage8_ui_demo_handoff_bundle.sh`

## Requirements
- Keep runtime truth unchanged:
  - rendered values must continue to come only from `get_ui_bootstrap_bundle.sh`
  - no invented metrics, fake dependencies, fake timelines, or markdown-derived runtime state
- Preserve the validated Stage 8 preview/handoff baseline:
  - do not remove embedded payload script `id="ui-bootstrap-payload"`
  - do not remove `data-section="overview"`, `data-section="visualization"`, `data-section="history"`
  - do not break existing smoke-suite expectations unless the smoke suite is updated in the same task with equivalent or stronger checks
- Apply the approved design direction from:
  - `docs/design/approved_figma_artifact.md`
  - `docs/design/artifacts/task076/`
  - `docs/design/artifacts/task074/`
- The preview must now visually express:
  - shared product shell
  - project identity/header
  - navigation continuity across overview / visualization / history
  - approved typography hierarchy
  - approved color and spacing rhythm
  - inspector-friendly technical product feel rather than a plain document page
- Scope is limited to shell/tokens/bootstrap framing only:
  - do not yet fully redesign Overview internals (`081`)
  - do not yet fully redesign Visualization workspace internals (`082`)
  - do not yet fully redesign History workspace internals (`083`)
- The resulting preview must remain desktop-first, readable, and consistent with the approved whole-application UI package.

## Acceptance Criteria
- Preview HTML generated by `render_ui_bootstrap_preview.sh` still validates against bootstrap/runtime contract expectations.
- Shared shell and visual-token layer are visibly upgraded to match the approved Stage 8 design direction.
- Existing section markers and embedded payload are preserved.
- Preview delivery smoke still passes after the UI shell/token update.
- `README` is updated with the new production-UI slice step.

## Manual Test (exact commands)
1. Resolve a real project id:
```bash
cd /Users/gelgard/PROJECTS/ContextViewer-1
PROJECT_ID="$(bash code/dashboard/get_project_list_overview_feed.sh | jq -r '.projects[0].project_id')"
printf 'PROJECT_ID=%s\n' "$PROJECT_ID"
```

2. Rebuild the standalone preview HTML:
```bash
bash code/ui/render_ui_bootstrap_preview.sh --project-id "$PROJECT_ID" --output /tmp/contextviewer_ui_preview/task080_preview.html
```

3. Confirm bootstrap markers and payload are still present:
```bash
rg -n 'data-section=\"overview\"|data-section=\"visualization\"|data-section=\"history\"|id=\"ui-bootstrap-payload\"' /tmp/contextviewer_ui_preview/task080_preview.html
```

4. Run the bootstrap contract smoke:
```bash
bash code/ui/verify_stage8_ui_bootstrap_contracts.sh --project-id "$PROJECT_ID"
```

5. Run the preview delivery smoke:
```bash
bash code/ui/verify_stage8_ui_preview_delivery.sh --project-id "$PROJECT_ID" --port 8787 --output-dir /tmp/contextviewer_ui_preview
```

6. Generate preview launch metadata:
```bash
bash code/ui/prepare_ui_preview_launch.sh --project-id "$PROJECT_ID" --output-dir /tmp/contextviewer_ui_preview
```

7. Open the generated preview in the browser:
```bash
open /tmp/contextviewer_ui_preview/contextviewer_ui_preview_${PROJECT_ID}.html
```

8. Capture screenshot evidence of the updated shell/token layer:
```bash
screencapture -x /tmp/contextviewer_ui_preview/task080_preview.png
ls -lh /tmp/contextviewer_ui_preview/task080_preview.png
```

9. Show changed files:
```bash
git status --short
```

## What to send back for validation
- `Changed files`
- Full output from steps 1–9
- The generated screenshot artifact:
  - `/tmp/contextviewer_ui_preview/task080_preview.png`
- Short confirmation covering all of:
  - project identity/header is visible
  - shell/navigation layer is visible
  - overview / visualization / history sections still appear in one unified product frame
  - result looks closer to approved `task076` design than the previous plain preview
- Final `git status --short`
