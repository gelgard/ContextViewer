# Data Layer (Stage 2)

This folder stores concrete data-layer implementation artifacts for ContextViewer.

Schema artifacts:
- `projects` / `snapshots` (AI Task 004+)
- `insert_snapshot_dedup(...)` — idempotent insert with filename and content-hash deduplication (AI Task 007)
- `snapshot_import_logs` / `insert_snapshot_import_log(...)` — per-project import/refresh status and messages (AI Task 008)

Out of scope for this folder:
- ingestion orchestration
- runtime snapshot selection
- dashboard / reporting APIs

## Stage 3 Ingestion

Read-only GitHub listing for `contextJSON` lives in `code/ingestion/github_contextjson_connector.sh` (GitHub Contents API → normalized JSON array of `*.json` files with `name`, `path`, `size`, `sha`, `download_url`). No database or import pipeline in that script.

`code/ingestion/contextjson_file_scanner.sh` reads that array from stdin, splits entries into `valid_files` / `invalid_files` using the `json_YYYY-MM-DD_HH-MM-SS.json` filename rule, and sets `latest_valid_file` to the row with the maximum extracted timestamp (no DB or runtime selection).

`code/ingestion/import_contextjson_pipeline.sh` chains connector → scanner, downloads each valid `download_url`, SHA-256-hashes the raw body, calls `insert_snapshot_dedup` / `insert_snapshot_import_log`, and prints one JSON summary (stdout). Requires `PROJECT_ID`, GitHub env vars, and `psql` connectivity.

`code/ingestion/refresh_contextjson_ingestion.sh` is the only supported entrypoint for running that pipeline in MVP: pass `manual_refresh` or `project_open`, get one JSON object with `trigger_source`, UTC `started_at` / `finished_at`, and nested `pipeline` (the pipeline summary). Invalid triggers do not run the importer; no cron/daemon logic.

`code/ingestion/get_project_import_status.sh` is read-only: given a numeric `project_id`, it queries `snapshot_import_logs` (latest row) and `snapshots` (count + max filename-derived `timestamp`) and prints one JSON object (`integration_status`, `latest_import_log`, etc.). No pipeline, network, or background work.

`code/ingestion/verify_stage3_ingestion_contracts.sh` runs contract smoke checks (connector → scanner → pipeline → refresh → import status) when `GITHUB_*`, `PROJECT_ID`, and `psql` connectivity are available, and prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Use `--help` for prerequisites; no daemons or UI.

## Stage 4 Interpretation

`code/interpretation/get_latest_valid_snapshot_projection.sh` is read-only: given a numeric `project_id`, it returns the newest `snapshots` row with `is_valid = true` ordered by filename-derived `timestamp` DESC (then `id` DESC), and prints one JSON object with `project_id`, `snapshot_id`, `snapshot_timestamp`, and `projection` (the row’s `raw_json`, or all nulls if none). No ingestion or network.

`code/interpretation/get_latest_snapshot_diff_summary.sh` is read-only: compares the two latest valid snapshots’ `raw_json` at **top-level keys only** (`added_*`, `removed_*`, `changed_*` arrays). With 0–1 valid snapshots, ids and diff arrays are empty where required; exit 0. No ingestion or network.

`code/interpretation/get_latest_changes_since_previous_projection.sh` is read-only: loads latest (+ previous id) valid snapshots (`timestamp` DESC, `id` DESC) and returns `changes_since_previous` and `changes_count` from the **latest** `raw_json` when that key is a JSON array; otherwise empty array and count 0. No ingestion or network.

`code/interpretation/get_latest_roadmap_progress_projection.sh` is read-only: latest valid snapshot only (`timestamp` DESC, `id` DESC) and returns `roadmap` (array) plus `progress` `{ implemented, in_progress, next }` (each array) with empty safe fallbacks if keys or types are wrong. No ingestion or network.

`code/interpretation/get_latest_current_status_projection.sh` is read-only: same latest valid snapshot selection and returns one JSON object with `project_id`, `latest_snapshot_id`, and `current_status` combining `progress` arrays (`implemented`, `in_progress`, `next`) with `changes_since_previous` (array from `raw_json` when valid). Missing snapshots or bad types fall back to empty arrays; no ingestion or network.

`code/interpretation/get_valid_snapshot_timeline_projection.sh` is read-only: lists all `snapshots` with `is_valid = true` for a numeric `project_id` as `timeline` (`snapshot_id`, `file_name`, `snapshot_timestamp`, `import_time`), ordered by `timestamp` DESC then `id` DESC, plus `total_valid_snapshots`. Empty project yields `0` and `[]`. No ingestion or network.

`code/interpretation/get_interpretation_bundle_projection.sh` is read-only: given a numeric `project_id`, runs the six Stage 4 interpretation scripts above (latest snapshot, diff summary, changes projection, roadmap/progress, current status, valid snapshot timeline) and prints one JSON object with `project_id`, `bundle_generated_at` (UTC), and each script’s full output under `latest_snapshot`, `diff_summary`, `changes_projection`, `roadmap_progress`, `current_status`, and `timeline`. No ingestion, network, or extra DB logic beyond those scripts.

`code/interpretation/get_dashboard_feed_projection.sh` is read-only: calls `get_interpretation_bundle_projection.sh` and prints a dashboard-oriented object (`project_id`, `generated_at`, `overview` with snapshot time, counts, and diff/changes metrics, plus `roadmap`, `progress` `{ implemented, in_progress, next }`, and `timeline` as the snapshot row array). Safe fallbacks when sections are empty or malformed. No ingestion or network.

`code/interpretation/verify_stage4_interpretation_contracts.sh` runs JSON contract smoke checks for the eight Stage 4 interpretation entrypoints (latest snapshot, diff summary, changes projection, roadmap/progress, current status, valid snapshot timeline, interpretation bundle, dashboard feed) given a numeric `project_id`, and prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Requires `jq` and working `psql` like the scripts under test; invalid `project_id` yields `fail` with `failed_checks > 0`. Exit code is non-zero when `status` is `fail`. No ingestion or network beyond DB connectivity.

## Stage 5 Dashboard Core

`code/dashboard/get_project_list_overview_feed.sh` is read-only: no arguments (optional `--help`); queries `projects`, latest `snapshot_import_logs` per project, and valid `snapshots` aggregates; prints one JSON object (`generated_at`, `total_projects`, `projects[]` with metadata, `latest_import_status` / `latest_import_time`, `latest_valid_snapshot_timestamp`, `total_valid_snapshots`). Sorted by `created_at` DESC then `project_id` DESC. Empty database yields `0` and `[]`. No ingestion or network.

`code/dashboard/get_project_overview_feed.sh` is read-only: given a numeric `project_id`, returns one JSON overview for that row (same import/snapshot fields as the list feed entry, plus `overview_generated_at`). Unknown id yields a clear stderr error and non-zero exit. No ingestion or network.

`code/dashboard/get_dashboard_home_feed.sh` is read-only: calls `get_project_list_overview_feed.sh` and prints `generated_at`, `summary` (`total_projects`, `projects_with_import_status`, `projects_with_valid_snapshots`), `projects`, and `selected_project_overview` (null unless `--project-id <id>` is set, then `get_project_overview_feed.sh`). Invalid CLI or unknown project id exits non-zero with stderr. No ingestion or network.

`code/dashboard/get_project_dashboard_feed.sh` is read-only: given a numeric `project_id`, calls `get_project_overview_feed.sh` (project must exist) then `get_dashboard_feed_projection.sh`, and prints `generated_at`, `project_overview`, and `dashboard_feed`. Invalid/missing id or child failure yields stderr + non-zero exit. No ingestion or network.

`code/dashboard/verify_stage5_dashboard_contracts.sh` runs JSON contract smoke checks for the four Stage 5 dashboard entrypoints (list overview, project overview, home feed with `--project-id`, project dashboard feed) plus negative exit checks for invalid `--invalid-project-id` (default `abc`). Requires `--project-id <id>` for positive checks and prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Exit code is non-zero when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/dashboard/get_dashboard_api_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer); runs the four dashboard scripts above and prints one JSON object (`generated_at`, `contracts` with `project_list_overview`, `project_overview`, `dashboard_home`, `project_dashboard`, and `consistency_checks` with `project_id_match` and `project_present_in_list`). Invalid/missing project or child failure yields stderr + non-zero exit. No ingestion or network.

## Stage 6 Visualization

`code/visualization/get_architecture_tree_feed.sh` is read-only: given a numeric `project_id`, ensures the project exists, then reads the latest valid snapshot’s `raw_json.architecture_tree` and prints `project_id`, `generated_at`, `snapshot_id` (or null), and a flattened `tree` (`path`, `type` as `file` or `directory`, `label`). Missing `architecture_tree` or no valid snapshot yields `tree` `[]` and `snapshot_id` null with exit 0. Unknown project exits non-zero. No ingestion or network.

`code/visualization/get_architecture_graph_feed.sh` is read-only: same snapshot selection as the tree feed; reads `raw_json.architecture_graph` and prints `project_id`, `generated_at`, `snapshot_id` (or null), and `graph` with normalized `nodes` (`id`, `label`, `type`) and `edges` (`source`, `target`, `relation`; accepts legacy `from`/`to` in source JSON). Empty or missing graph data yields empty arrays with exit 0. Unknown project exits non-zero. No ingestion or network.

`code/visualization/verify_stage6_visualization_contracts.sh` runs JSON contract smoke checks for `get_architecture_tree_feed.sh` and `get_architecture_graph_feed.sh` (positive shape + negative invalid id), given `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_bundle_feed.sh` is read-only: one positional `project_id` (non-negative integer) and optional `-h`/`--help`. Runs `get_architecture_tree_feed.sh` and `get_architecture_graph_feed.sh` and prints one JSON object with `project_id`, `generated_at`, full `architecture_tree` and `architecture_graph` child outputs, and `consistency_checks` (`project_id_match`, `snapshot_id_match` between the two payloads). Invalid id or missing project propagates child stderr and non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_runtime_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer) and `-h`/`--help`. Calls `code/dashboard/get_project_overview_feed.sh` and `get_visualization_bundle_feed.sh`, and prints one lightweight JSON object (`generated_at`, `project_id`, `project_overview` subset with `project_id`, `name`, `latest_valid_snapshot_timestamp`, `total_valid_snapshots`, `visualization` subset with `snapshot_id`, `tree`, `graph.nodes`, `graph.edges`, and `consistency_checks` with `project_id_match`, `snapshot_id_match`). No smoke or contract bundles. Missing/invalid `--project-id` or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_runtime_contracts.sh` runs JSON contract smoke checks for `get_visualization_runtime_feed.sh`, `get_project_visualization_feed.sh`, and `get_visualization_home_feed.sh --project-id`, plus negative exit checks for invalid/missing `--project-id` on `get_visualization_runtime_feed.sh`. Requires `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_api_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`) for the verify step, and `-h`/`--help`. Runs `get_architecture_tree_feed.sh`, `get_architecture_graph_feed.sh`, `get_visualization_bundle_feed.sh`, and `verify_stage6_visualization_contracts.sh` and prints one JSON object (`generated_at`, `contracts` with `architecture_tree`, `architecture_graph`, `visualization_bundle`, `visualization_contract_smoke`, and `consistency_checks` with `project_id_match`, `snapshot_id_match`, `smoke_status_pass`). Missing/invalid `--project-id` or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/get_project_visualization_feed.sh` is read-only: one positional `project_id` (non-negative integer) and optional `-h`/`--help`. Calls `code/dashboard/get_project_overview_feed.sh` and `get_visualization_api_contract_bundle.sh --project-id <id>`, and prints one JSON object (`generated_at`, `project_overview`, `visualization`, `consistency_checks` with `project_id_match`, `snapshot_alignment`, `smoke_status_pass`). Invalid id, missing project, or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_home_feed.sh` is read-only: no arguments or optional `--project-id <id>` and `-h`/`--help`. Calls `code/dashboard/get_project_list_overview_feed.sh` and, when `--project-id` is set, `get_project_visualization_feed.sh` for that id. Prints one JSON object (`generated_at`, `summary` with `total_projects`, `projects_with_import_status`, `projects_with_valid_snapshots`, `projects_with_visualization_data`, `projects`, `selected_project_visualization` object or null). Without `--project-id`, `selected_project_visualization` is null. Invalid/missing project or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_api_contracts.sh` runs JSON contract smoke checks for the five Stage 6 visualization/API entrypoints (`get_architecture_tree_feed.sh`, `get_architecture_graph_feed.sh`, `get_visualization_bundle_feed.sh`, `verify_stage6_visualization_contracts.sh`, `get_visualization_api_contract_bundle.sh`) plus negative exit checks for invalid `--invalid-project-id` on `get_visualization_bundle_feed.sh` and `get_visualization_api_contract_bundle.sh`, given `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_home_contracts.sh` runs JSON contract smoke checks for `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh` (base and with `--project-id`), and `get_visualization_api_contract_bundle.sh`, plus negative exit checks for invalid ids on `get_project_visualization_feed.sh` and `get_visualization_home_feed.sh --project-id`. Requires `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_workspace_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`) for the API bundle and smoke suites, and `-h`/`--help`. Aggregates `get_visualization_home_feed.sh` (base and with `--project-id`), `get_project_visualization_feed.sh`, `get_visualization_api_contract_bundle.sh`, `verify_stage6_visualization_contracts.sh`, `verify_stage6_visualization_api_contracts.sh`, and `verify_stage6_visualization_home_contracts.sh`, and prints one JSON object (`generated_at`, `contracts` with `visualization_home_base`, `visualization_home_selected`, `project_visualization`, `visualization_api_bundle`, `visualization_smoke`, `visualization_api_smoke`, `visualization_home_smoke`, and `consistency_checks` with `project_id_match`, `snapshot_id_match`, `all_smokes_pass`). Missing/invalid `--project-id`, unknown project, strict child failure, or failed consistency checks yields stderr and/or non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_workspace_contracts.sh` runs JSON contract smoke checks for `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh` (base and with `--project-id`), `get_visualization_api_contract_bundle.sh`, and `get_visualization_workspace_contract_bundle.sh`, plus negative exit checks for invalid ids on `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh --project-id`, and `get_visualization_workspace_contract_bundle.sh --project-id`. Requires `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/benchmark_stage6_visualization_latency.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--iterations <n>` (integer >= 1, default `1`), and `-h`/`--help`. Times wall-clock latency (milliseconds) for `get_architecture_tree_feed.sh`, `get_architecture_graph_feed.sh`, `get_visualization_bundle_feed.sh`, `get_visualization_api_contract_bundle.sh`, `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh --project-id`, and `get_visualization_workspace_contract_bundle.sh --project-id`, and prints one JSON report (`status`, `project_id`, `iterations`, `generated_at`, `benchmarks[]` with `name`, `iterations`, `durations_ms`, `avg_ms`, `max_ms`, `status`, `error`, and `summary` with `total_checks`, `failed_checks`, `slowest_check_name`, `slowest_check_max_ms`). Invalid CLI, missing scripts, or any failed benchmark yields stderr and/or non-zero exit. Uses `python3` or `perl` for millisecond timing. No ingestion or network beyond DB connectivity.

`code/visualization/check_stage6_visualization_latency_guardrails.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--iterations <n>` (integer >= 1, default `1`), optional `--max-ms <value>` (integer >= 1, default `120000`), and `-h`/`--help`. Runs `benchmark_stage6_visualization_latency.sh` with the same `--project-id` and `--iterations`, then fails if any benchmark entry has `status != "ok"` or `max_ms > threshold_max_ms`. Prints one JSON (`status`, `project_id`, `iterations`, `threshold_max_ms`, `generated_at`, `benchmark` full output, `violations` with `name`, `max_ms`, `reason`, and `summary` with `checks_total`, `violations_total`, `slowest_check_name`, `slowest_check_max_ms`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_stage6_visualization_readiness_report.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--iterations <n>` (default `1`), `--max-ms <value>` (default `120000`), `--invalid-project-id <value>` (default `abc`), and `-h`/`--help`. Runs `check_stage6_visualization_latency_guardrails.sh`, `verify_stage6_visualization_runtime_contracts.sh`, and `verify_stage6_visualization_workspace_contracts.sh`, and prints one JSON object (`generated_at`, `project_id`, `ready_for_runtime`, `readiness_checks` with `latency_guardrails_pass`, `runtime_contracts_pass`, `workspace_contracts_pass`, and `details` with full child payloads). `ready_for_runtime` is true only when all three child `status` values are `pass`. Exit 0 when `ready_for_runtime` is true; non-zero otherwise. Invalid CLI yields stderr without JSON. No ingestion or network beyond DB connectivity.

`code/visualization/get_stage6_completion_gate_report.sh` is read-only: same CLI as `get_stage6_visualization_readiness_report.sh`. Calls `get_stage6_visualization_readiness_report.sh` with the same arguments and prints one JSON object (`generated_at`, `project_id`, `stage` set to `Stage 6`, `can_transition_to_stage7`, `gate_checks` with `runtime_ready`, `latency_within_threshold`, `runtime_contracts_pass`, `workspace_contracts_pass`, `blocking_reasons` string array, and `readiness_report` full payload). `can_transition_to_stage7` is true only when all gate checks are true. Exit 0 when `can_transition_to_stage7` is true; non-zero otherwise. No ingestion or network beyond DB connectivity.

## Stage 7 History

`code/history/get_project_history_daily_rollup_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional inclusive UTC day bounds `--from` / `--to` (`YYYY-MM-DD`), and `-h` / `--help`. Queries `snapshots` with `is_valid = true` for that project, groups by UTC calendar day derived from the filename-derived `timestamp` column (same notion as Stage 4 timeline). Prints one JSON object: `project_id`, `generated_at` (UTC), `range` `{ from, to }` (each string or `null` when the flag is omitted), `summary` with `days_with_activity`, `total_valid_snapshots` (equals the sum of `days[].valid_snapshots_count`), and `latest_snapshot_timestamp` (string or `null`), plus `days[]` sorted by `date` descending with `date`, `valid_snapshots_count`, `latest_snapshot_timestamp`, and `snapshot_ids` (numeric ids, newest snapshot first within each day). Invalid `--project-id` format → stderr, exit `1`; project not found → stderr, exit `4`; invalid dates or `from > to` → stderr, exit `1`; database/query failure → stderr, exit `3`. Depends on `psql`, `jq`, and `python3` for calendar date validation. No ingestion or network beyond DB connectivity.

`code/history/get_project_history_timeline_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional inclusive UTC day bounds `--from` / `--to` (`YYYY-MM-DD`), optional `--limit <n>` (integer ≥ 1, default `200`), and `-h` / `--help`. Queries `snapshots` with `is_valid = true` for that project, ordered by filename-derived `timestamp` DESC then `id` DESC, capped at `--limit`. Prints one JSON object: `project_id`, `generated_at` (UTC), `range` `{ from, to, limit }` (`from` / `to` string or `null` when omitted; `limit` is the requested cap), `summary` with `total_returned` (equals `timeline | length`), `latest_snapshot_timestamp`, and `oldest_snapshot_timestamp` (each among returned rows, or `null` when empty), and `timeline[]` with `snapshot_id`, `file_name`, `snapshot_timestamp`, `import_time` (UTC string or `null`), and `day` (`YYYY-MM-DD`, same UTC-day notion as the daily rollup). Invalid `--project-id` format → stderr, exit `1`; project not found → stderr, exit `4`; invalid dates, `from > to`, or invalid `--limit` → stderr, exit `1`; database/query failure → stderr, exit `3`. Depends on `psql`, `jq`, and `python3` for calendar date validation. No ingestion or network beyond DB connectivity.

`code/history/get_project_history_bundle_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--from` / `--to` (`YYYY-MM-DD`), optional `--limit <n>` (integer ≥ 1, default `200`; passed to the timeline feed only), and `-h` / `--help`. Invokes `get_project_history_daily_rollup_feed.sh` and `get_project_history_timeline_feed.sh` with the same `--project-id` and date bounds, and prints one JSON object: `project_id`, `generated_at` (UTC for the bundle), `range` (copied from the timeline payload: `{ from, to, limit }`), `history` with full `daily` and `timeline` child outputs, and `consistency_checks` (`project_id_match`, `range_match` on `from`/`to`, `timeline_count_consistent`, `latest_timestamp_aligned`). Invalid CLI → stderr, non-zero exit; missing project or child failure → child stderr and exit code propagated; malformed child JSON or failed internal consistency checks → stderr, exit `3`. Depends on `jq` and executable child scripts. No ingestion or network beyond DB connectivity.

`code/history/verify_stage7_history_contracts.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc` for negative exit checks), and `-h` / `--help`. Runs JSON contract smoke checks for `get_project_history_daily_rollup_feed.sh`, `get_project_history_timeline_feed.sh`, and `get_project_history_bundle_feed.sh` (positive: exit `0` and shape validation; bundle must have all `consistency_checks` true), plus three negative checks expecting exit `1` when each script is invoked with invalid `--project-id`. Prints one JSON object: `status` (`pass` or `fail`), `checks` (`{ name, status, details }` per check), `failed_checks`, `generated_at` (UTC). Invalid top-level `--project-id` (not a non-negative integer) → stdout only: `status` `fail`, `failed_checks` `1`, check `project_id`, exit `1`. Missing `--project-id` or unknown args → stderr, non-zero exit. Exit `0` only when `status` is `pass`. Depends on `jq` and the three history scripts. No ingestion or network beyond DB connectivity.

`code/history/get_history_home_feed.sh` is read-only: no arguments (base payload) or optional `--project-id <id>` (non-negative integer) and `-h` / `--help`. Calls `code/dashboard/get_project_list_overview_feed.sh`, and when `--project-id` is set calls `get_project_history_bundle_feed.sh --project-id <id>`. Prints one JSON object: `generated_at` (UTC), `summary` with `total_projects`, `projects_with_valid_snapshots`, `projects_with_history_data` (count where `latest_valid_snapshot_timestamp` is not null), `projects` (same array as the list feed), `selected_project_history` (full bundle JSON or `null` when no project selected), and `consistency_checks` (`summary_total_matches_projects_length`, `selected_project_id_match`, `selected_history_consistent`). Invalid `--project-id` format → stderr, exit `1`; unknown project or bundle failure → child stderr and exit code propagated; list feed failure or invalid JSON → stderr, exit `3`; failed root `consistency_checks` after successful children → stderr, exit `3`. Depends on `jq` and child scripts. No ingestion or network beyond DB connectivity.

`code/history/get_history_workspace_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`, forwarded to `verify_stage7_history_contracts.sh`), and `-h` / `--help`. Aggregates `get_history_home_feed.sh` (base), `get_history_home_feed.sh --project-id <id>`, `get_project_history_bundle_feed.sh --project-id <id>`, and `verify_stage7_history_contracts.sh --project-id <id> --invalid-project-id <value>`. Prints one JSON object: `generated_at` (UTC), `contracts` with `history_home_base`, `history_home_selected`, `project_history_bundle`, `history_api_smoke`, and `consistency_checks` (`project_id_match`, `selected_bundle_match`, `history_smoke_pass`). Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; unknown project or non-smoke child failure → propagated exit; invalid JSON from a required child → stderr, exit `3`; any `consistency_checks` false after successful JSON assembly → stdout JSON then exit `3`. The smoke suite’s JSON is always captured (even when its exit status is non-zero); `history_smoke_pass` requires `history_api_smoke.status == "pass"`. Depends on `jq` and executable child scripts. No ingestion or network beyond DB connectivity.

## Stage 8 Polish

### Stage 8 Figma design branch (documentation)

`docs/design/figma_design_branch_charter.md` is the **charter** for the Figma branch after the validated preview/handoff checkpoint: it preserves that checkpoint, separates **runtime truth** (JSON / contextJSON) from **design truth** (approved Figma), and states that the local agent **authors prompts for a third-party Figma-generation system**—not final shipped UI. `docs/design/figma_prompt_workflow.md` is the **workflow**: exact task chain **062**–**079** (conditional **073** for IA fallback; active continuation **074**–**079**), **superseded** in-repo placeholders **067**–**072** (not valid execution anchors), mandatory returned artifacts for design-validation tasks **064 / 066 / 075 / 077**, and required visual manual tests (viewing steps, confirmation checklist, screenshot command + path). **AI Task 062** delivers these two docs; executable scripts in this section are unchanged.

**AI Task 063** adds the first copy-paste **product/UI brief** prompt pack for that external system: `docs/design/prompts/figma_product_ui_brief_prompt.md` (English, copy-ready; overview / visualization workspace / history workspace; validated preview–handoff baseline; anti-goals including **no generic SaaS dashboard**, no fake analytics, no invented data, no modal-heavy UX) and `docs/design/prompts/figma_product_ui_brief_submission_checklist.md` (what to bring back: full prompt, Figma URL/file/export, system text, page/frame list, per-page screenshots/exports, product-specificity note). Validation is **AI Task 064**.

**AI Task 064** records the formal review of returned product-brief design artifacts in `docs/design/reviews/figma_product_ui_brief_validation.md`, including artifact inventory, pass/fail verdict, strengths, defects, corrections, and Go/No-Go for the next design-branch step.

The reviewed external artifacts themselves are now preserved in `docs/design/artifacts/task064/`:
- raw bundle: `ContextViewer.zip`
- native design file: `ContextViewer.fig`
- PDF export: `ContextViewer.pdf`
- extracted screens, HTML handoff files, and design-system notes

This preserved package is the current **near-complete core UI baseline** for the Figma branch. It is strong enough to continue the design workflow, but it is not yet the final exhaustive approved artifact for architecture import.

**AI Task 065** adds a copy-paste **information architecture** prompt pack for the same third-party Figma-generation flow: `docs/design/prompts/figma_information_architecture_prompt.md` (English; uses **Task 064** baseline + `docs/design/artifacts/task064/README.md` + `docs/design/reviews/figma_product_ui_brief_validation.md` as input context; preserves **Overview** as default entry, **Visualization workspace** with tree+graph **in one workspace**, **History workspace** first-class; global shell, page map, navigation model, workspace relationship map, inspector / empty / return-navigation behavior, screen-to-screen diagram, demo / investor / product journeys; explicit anti–**overloaded dashboard**, anti–**modal-heavy IA**, no flows that contradict **JSON/runtime** semantics; instructs output: page hierarchy, navigation model, workspace map, key frame list, IA rationale). Submission requirements: `docs/design/prompts/figma_information_architecture_submission_checklist.md` (exact prompt, Figma URL/file/id, IA text, **page map** export, **navigation/flow** exports, frame/page list, product-specific **implementation-ready** note).

**AI Task 066** records formal **information architecture** validation in `docs/design/reviews/figma_information_architecture_validation.md`: input prompt reference, artifact inventory, navigation summary, strengths/defects, exact corrections, explicit **Verdict** (`pass` / `fail`), and **Go / No-Go for AI Task 074** (visual system prompt pack — after numbering correction). It checks overview entry, visualization/history placement, unified visualization workspace, inspector behavior, progressive disclosure, modal/clutter load, and ContextViewer-specific navigation. **Revision 2** (after **AI Task 073**) uses **architecture-derived fallback evidence** under `docs/design/artifacts/task065/` with **workspace-only** authority (**external Figma URLs** not primary evidence) and records **`pass` / `GO`** for **074** — IA structure is certified; native external full raster exports are **not** claimed.

**AI Task 073** assembled that fallback package: `docs/design/artifacts/task065/` (`README.md`, `PROMPT_USED.md`, `IA_RESULT.md`, `page_map.md`, `navigation_model.md`, `frame_page_list.md`, `exports/page_map.mmd`, `exports/navigation_flow.mmd`). It is **not** represented as a native full export from the external Figma-generation system; it closes the **065** evidence gap so **066** can re-open honestly before **074**.

**AI Task 074** adds the copy-paste **visual system** prompt pack for the same third-party Figma-generation flow: `docs/design/prompts/figma_visual_system_prompt.md` (English; inputs **064** baseline + **065** certified IA + preserved **061** checkpoint; typography, color, spacing rhythm, component language, panel/card treatment, shell/navigation, inspector/secondary styling, empty/loading/error/sparse/populated states, desktop-first + mobile degradation; product character **technical / dense / architecture-aware**; forbids **purple-on-white** defaults, **generic startup dashboard** style, decorative non-contract charts, random non-specific illustration, visual detachment of visualization/history from unified shell; required outputs: rationale, type/color/spacing tokens, component inventory, example frames). Submission: `docs/design/prompts/figma_visual_system_submission_checklist.md` (exact prompt, Figma URL/upload/canonical id, rationale, tokens, inventory, screenshots/exports, product-specific **implementation-ready** note). Validation is **AI Task 075**.

**AI Task 075** records formal **visual system** validation in `docs/design/reviews/figma_visual_system_validation.md`: input prompt reference, **artifact inventory**, **visual strengths** / **visual defects**, **product-fit evaluation**, exact **corrections**, explicit **Verdict** (`pass` / `fail`), and **Go / No-Go for AI Task 076** (screen prompt pack). It assesses product-specificity vs generic SaaS, dense readable technical UI, hierarchy across shell/overview/visualization/history, **IA** compatibility (`task065`), unified shell for viz/history, inspector coherence, **state coverage** (loading/empty/error/sparse/populated), and implementation-ready vs moodboard output. **Revision 2** uses the workspace-registered fallback evidence package under `docs/design/artifacts/task074/` and records **`pass` / `GO`** for **076**; this package is documentation-derived from uploaded workspace artifacts and does not claim native external visual-system export parity.

**AI Task 076** adds the **screen-by-screen** copy-paste prompt pack for the same third-party Figma-generation flow: `docs/design/prompts/figma_screen_prompts.md` (English; five separate fenced blocks — **shared shell/navigation**, **overview/home**, **visualization workspace** tree+graph+inspector, **history workspace**, **demo/handoff presentation mode**; each with purpose, data-bearing regions, interaction zones, empty/sparse/populated/loading/error where relevant, cross-screen links; contract-backed — no fake metrics/timelines/dependencies/widgets; preserves overview entry, unified visualization, first-class history, shell continuity, progressive disclosure). Submission: `docs/design/prompts/figma_screen_prompt_submission_checklist.md` (exact blocks used, Figma URL/upload/canonical id, frame/page names, per-screen screenshots/exports, missing/weak screens note, IA alignment note). Validation is **AI Task 077**.

**AI Task 077** records formal **screen-level** validation in `docs/design/reviews/figma_screen_validation.md`: **artifact inventory**, **returned Figma file reference**, **visible screen list**, validations for **overview**, **visualization**, **history**, **shell/navigation**, **demo-mode**, **defects**, **Corrections** list, **Verdict** (approval / rejection), and **Go / No-Go for AI Task 078**. Explicit checks: project **name/id** visibility, overview signal quality, viz/history **usability**, **unified** product feel, **demo readiness**, **shell continuity**, alignment with certified **IA** (**065**) and **visual system** (**074**), **contract honesty** (no fake metrics, dependencies, timelines). Includes **visual manual test** (exact viewing steps, confirmation checklist, `screencapture` + repo `exports/` paths). **Revision 2** uses the workspace-registered screen evidence package under `docs/design/artifacts/task076/` (uploaded raw `.zip` / `.fig` / `.pdf` plus preserved local extracted screens from the same design lineage), records **`approve` / `GO`** for **078**, and promotes that package into the current **base UI package for the whole application** until superseded by a later approved import.

**AI Task 078** — **Figma import and architecture sync:** creates **`docs/design/approved_figma_artifact.md`** as the **canonical authoritative UI design reference** (primary package **`docs/design/artifacts/task076/`**; **workspace uploads** are primary evidence; external Figma URLs optional only); synchronizes **`docs/architecture/system-definition.md`** §20, **`AGENTS.md`**, **plans**, **recovery**, **`code/data_layer/README.md`**, and a new **contextJSON** snapshot so **runtime truth** remains **latest contextJSON + JSON contracts** while **design truth** points at the approved record.

**AI Task 079** — **Post-Figma implementation plan refinement:** updates **`docs/plans/system-implementation-plan.md`**, **`docs/plans/implementation-plan.md`**, **`docs/plans/product_goal_traceability_matrix.md`**, **`project_recovery/*`**, and this **`README`** so Stage 8 enters **post-Figma production UI** mode: **design-sync** tasks **062–079** are closed; **preserved baseline** remains validated preview/demo handoff through **`061`**; **design authority** stays **`approved_figma_artifact.md`** + **`task076/`**; **next** numbered slices are **`080–083`** (shell/tokens → overview → visualization → history + handoff smoke) per **`docs/plans/implementation-plan.md`** §Post-Figma roadmap — executable shell commands and visual manual-test discipline documented there (**architecture-first**: no silent JSON semantic changes).

**AI Task 080** — **Preview shell + design tokens (bootstrap):** **`render_ui_bootstrap_preview.sh`** gains the approved **Monolith Slate–style** shared chrome (see **`approved_figma_artifact.md`**, **`task076/`**): project header, workspace sidebar linking the three sections, typography/spacing/color tokens. **`id="ui-bootstrap-payload"`** and **`data-section`** markers (`overview`, `visualization`, `history`) are unchanged; bootstrap JSON semantics unchanged. **`data-cv-preview-shell="080"`** on `<body>` is enforced by **`verify_stage8_ui_preview_delivery.sh`**.

**AI Task 081** — **Overview surface fidelity (bootstrap preview):** extends **`render_ui_bootstrap_preview.sh`** so the **Overview** `data-section` renders **`overview-surface`** **high-signal blocks** (current status metrics, plan progress columns **Implemented / In progress / Next**, roadmap list, recent snapshot timeline table, GitHub + import line) using **only** `ui_sections.overview.project_overview` and `ui_sections.overview.dashboard_feed` from `get_ui_bootstrap_bundle.sh` — no Markdown, no invented KPIs. **Shell** from **080** unchanged (`data-cv-preview-shell="080"`, sidebar, markers, payload).

**AI Task 082** — **Visualization workspace fidelity (bootstrap preview):** same script; **`data-section="visualization"`** becomes one **`viz-workspace`** **unified surface** (CSS grid): feed-backed **architecture tree** list (`project_visualization.visualization.contracts.architecture_tree`), **graph** node/edge tables (`architecture_graph`), **inspector** readout for the **first tree row** only (no interactive selection in static HTML). Shows workspace bundle time, snapshot ids, row/node/edge **counts** (= `len` of feed arrays), and visualization_workspace **consistency_checks**. No invented graph analytics. Stdout **`render_profile`**: **`082_visualization_fidelity`**.

**AI Task 083** — **History workspace fidelity + cross-surface handoff smoke (bootstrap):** same script; **`data-section="history"`** renders a **`history-workspace`** surface (daily rollup + snapshot timeline + workspace/bundle **consistency_checks** readouts) using **only** `ui_sections.history_workspace` and nested **`contracts.project_history_bundle`**, **`history.daily`**, **`history.timeline`** from `get_ui_bootstrap_bundle.sh` — no invented timeline analytics or markdown-derived runtime state. Stdout **`render_profile`**: **`083_history_handoff_fidelity`**. **`verify_stage8_ui_demo_handoff_bundle.sh`** (ready bundle + **HTTP 200** on **`handoff.preview_url`**) and **`verify_stage8_ui_preview_delivery.sh`** assert served HTML contains **`class="overview-surface"`**, **`class="viz-workspace"`**, and **`class="history-workspace"`** alongside existing section/payload/shell markers. **`get_stage8_ui_preview_readiness_report.sh`** includes top-level **`render_profile`** from prepare. Completes the **Stage 8 production UI** slice **`080–083`**.

`code/ui/get_ui_bootstrap_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`, passed to both workspace bundles), and `-h` / `--help`. Aggregates `code/dashboard/get_project_dashboard_feed.sh` with a **positional** project id (same as Stage 5 — not `--project-id`), `get_visualization_workspace_contract_bundle.sh --project-id <id> --invalid-project-id <value>`, and `get_history_workspace_contract_bundle.sh --project-id <id> --invalid-project-id <value>`. Prints one JSON object: `generated_at` (UTC), `project_id`, `ui_sections` with `overview` (full dashboard feed: `project_overview` + `dashboard_feed`), `visualization_workspace` (full Stage 6 workspace bundle), `history_workspace` (full Stage 7 workspace bundle), and `consistency_checks` (`project_id_match`, `overview_present`, `visualization_consistent`, `history_consistent`). Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; unknown project or child failure → propagated exit; invalid JSON from any child → stderr, exit `3`; any root `consistency_checks` false → stdout JSON then exit `3`. Depends on `jq` and executable child scripts. No ingestion or network beyond DB connectivity.

`code/ui/verify_stage8_ui_bootstrap_contracts.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`, passed to `get_ui_bootstrap_bundle.sh` for the positive run and for the negative invalid-id check), and `-h` / `--help`. Runs `get_ui_bootstrap_bundle.sh --project-id <id> --invalid-project-id <value>` and validates exit `0`, JSON shape (`project_id`, `generated_at`, `ui_sections.overview` / `visualization_workspace` / `history_workspace`, boolean `consistency_checks.*`), and that all bootstrap consistency flags are `true`; also runs negative checks on the bootstrap script (no args → expected exit `2`; `--project-id <invalid>` → expected exit `1`). Prints one JSON object: `status` (`pass` or `fail`), `checks` (`{ name, status, details }` per check), `failed_checks`, `generated_at` (UTC). Missing `--project-id` on this script or unknown args → stderr, exit `2`. Invalid top-level `--project-id` (not a non-negative integer) → stdout only: `status` `fail`, `failed_checks` `1`, check `project_id`, exit `1`. Exit `0` only when `status` is `pass`. Depends on `jq` and `get_ui_bootstrap_bundle.sh`. No ingestion or network beyond DB connectivity.

`code/ui/render_ui_bootstrap_preview.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), `--output <path>` (destination HTML file), optional `--invalid-project-id <value>` (default `abc`, passed to `get_ui_bootstrap_bundle.sh`), and `-h` / `--help`. Calls `get_ui_bootstrap_bundle.sh`, then `code/diff/get_diff_viewer_contract_bundle.sh` and `code/settings/get_settings_profile_contract_bundle.sh` (`--project-id <id>`), merges **`ui_sections.diff_viewer`** and **`ui_sections.settings_profile`**, and writes one self-contained HTML file (inline CSS only; `data-section` markers `overview`, `visualization`, `history`, **`diff`**, **`settings`**; embedded JSON in `<script type="application/json" id="ui-bootstrap-payload">` with `</script>`-safe escaping). **AI Task 080** — **Monolith Slate–style** shell: shared chrome, sidebar nav (**088**: five links incl. Diff + Settings / profile), tokens. **AI Tasks 081–083** — surface roots unchanged. **AI Task 085** — **`diff-workspace`** (`data-cv-diff-surface="085"`) from **`get_diff_viewer_contract_bundle.sh` only**. **AI Task 088** — **`settings-workspace`** (`data-cv-settings-surface="087"`) from **`get_settings_profile_contract_bundle.sh` only**. `data-cv-preview-shell="080"` unchanged on `<body>`. Stdout JSON: `sections_rendered` includes `diff` and `settings`, `render_profile` **`088_stage9_secondary_flows_preview`**, `source_consistency_checks` (bootstrap + nested **`diff_viewer`** + **`settings_profile`**), **`diff_viewer_state`**, **`settings_surface_state`** (`available`, `contract_consistent`, read-only flags). Child contract failures abort render. Depends on `jq`, `python3`, `get_ui_bootstrap_bundle.sh`, **`get_diff_viewer_contract_bundle.sh`**, **`get_settings_profile_contract_bundle.sh`**. No ingestion or network beyond DB connectivity.

`code/ui/prepare_ui_preview_launch.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to `render_ui_bootstrap_preview.sh`), and `-h` / `--help`. Ensures `--output-dir` exists (`mkdir -p`), then calls `render_ui_bootstrap_preview.sh` with `--output <output-dir>/contextviewer_ui_preview_<project-id>.html`. Prints one JSON object: `project_id`, `generated_at`, `output_dir` (absolute), `output_file` (absolute), `open_command` (macOS `open` string), `preview_summary` with `sections_rendered`, `source_consistency_checks`, `render_profile` (**`088_stage9_secondary_flows_preview`** after **AI Task 088**), **`diff_viewer_state`**, and **`settings_surface_state`**. Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; render failure → propagated exit; `mkdir` / directory resolution failure or invalid child JSON → stderr, exit `3`. Depends on `jq` and `render_ui_bootstrap_preview.sh`. No ingestion or network beyond DB connectivity.

`code/ui/start_ui_preview_server.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), optional `--port <n>` (integer ≥ `1`, default `8787`), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to `prepare_ui_preview_launch.sh`), and `-h` / `--help`. Calls `prepare_ui_preview_launch.sh`, then starts `python3 -m http.server` on `--port` with document root `--output-dir` (uses `--directory` when supported, otherwise `cd` + server), via `nohup` in the background, and verifies `HTTP 200` on the preview URL with `curl`. Prints one JSON object: `project_id`, `generated_at`, `output_dir`, `output_file`, `server_url` (`http://127.0.0.1:<port>/`), `preview_url` (`http://127.0.0.1:<port>/contextviewer_ui_preview_<id>.html`), `server_command` (exact `nohup` line used), `open_command` (`open` + quoted `preview_url`). Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` or invalid `--port` (< `1` or non-numeric) → stderr, exit `1`; prepare failure → propagated exit; missing preview file, invalid prepare JSON, or server not ready → stderr, exit `3`. Depends on `jq`, `python3`, `curl`, and `prepare_ui_preview_launch.sh`. Leaves a background `http.server` process listening (until stopped). No ingestion or network beyond DB + localhost HTTP.

`code/ui/verify_stage8_ui_preview_delivery.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), optional `--port <n>` (integer ≥ `1`, default `8787`), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to `start_ui_preview_server.sh` and `verify_stage8_ui_bootstrap_contracts.sh`), and `-h` / `--help`. Runs `start_ui_preview_server.sh` with the same `--project-id`, `--port`, `--output-dir`, and `--invalid-project-id`, validates exit `0` and the full server JSON shape, asserts `preview_url`, checks `HTTP 200` via `curl`, verifies served HTML contains `data-section` for **overview**, **visualization**, **history**, **diff**, **settings**, `id="ui-bootstrap-payload"`, `data-cv-preview-shell="080"`, `class="overview-surface"`, `class="viz-workspace"`, `class="history-workspace"`, **`class="diff-workspace"`**, and (**AI Task 088**) **`class="settings-workspace"`** (temp file after `curl`), then runs `verify_stage8_ui_bootstrap_contracts.sh` (exit `0` and `status: pass`). Negative CLI checks as before on this script and `start_ui_preview_server.sh`. Prints one JSON report; exit `0` only when `status` is `pass`. Depends on `jq`, `curl`, and children. No ingestion or network beyond DB + localhost HTTP.

`code/ui/get_stage8_ui_preview_readiness_report.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), optional `--port <n>` (integer ≥ `1`, default `8787`), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to all children), and `-h` / `--help`. Calls `prepare_ui_preview_launch.sh`, `verify_stage8_ui_bootstrap_contracts.sh`, and `verify_stage8_ui_preview_delivery.sh`, embedding child JSON under `verification`. Prints one JSON object: `project_id`, `generated_at` (UTC), `status` (`ready` or `not_ready`), `preview_artifacts`, `render_profile` (**`088_stage9_secondary_flows_preview`** after **AI Task 088**), `verification`, `readiness_summary` (overview / visualization / history / **diff** / **settings** availability flags incl. **`settings_profile_available`**, **`settings_contract_consistent`**, **`settings_read_only_flags_ok`**, **`investor_demo_ready`** — requires **diff** and **settings** surfaces and `preview_summary` contract flags), and `consistency_checks`. Same exit semantics as before (exit `3` when `not_ready`). Depends on `jq` and children. No ingestion or network beyond DB + localhost HTTP.

`code/ui/get_stage8_ui_demo_handoff_bundle.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), optional `--port <n>` (integer ≥ `1`, default `8787`), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to all children), and `-h` / `--help`. Calls `prepare_ui_preview_launch.sh`, `start_ui_preview_server.sh`, and `get_stage8_ui_preview_readiness_report.sh` with the same `--project-id`, `--port`, `--output-dir`, and `--invalid-project-id`. Prints one JSON object: `project_id`, `generated_at` (UTC), `status` (`ready` or `not_ready`), `handoff` (paths, URLs, commands, **`demo_steps`** — **six** ordered strings incl. **settings/profile**), `readiness`, and `consistency_checks`. Same ready / exit semantics as before. Depends on `jq` and the three child scripts. No ingestion or network beyond DB + localhost HTTP.

`code/ui/verify_stage8_ui_demo_handoff_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--port <n>` (integer ≥ `1`, default `8787`), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to `get_stage8_ui_demo_handoff_bundle.sh`), and `-h` / `--help`. Runs `get_stage8_ui_demo_handoff_bundle.sh` with the same flags and validates exit `0`, full handoff JSON shape, `status == "ready"`, all `consistency_checks` booleans true, **six** ordered `demo_steps`, `preview_url` containing `127.0.0.1`, and non-empty `browser_open_command`. Requires `curl`. When the bundle is **ready**, **`GET`s** `handoff.preview_url` and, on **HTTP 200**, asserts served HTML contains **`class="overview-surface"`** (081), **`class="viz-workspace"`** (082), **`class="history-workspace"`** (083), **`data-section="diff"`**, **`class="diff-workspace"`** (085), **`data-section="settings"`**, and **`class="settings-workspace"`** (088). Negative exit checks on the handoff script as before. Prints one JSON report; exit `0` only when `status` is `pass`. Depends on `jq`, `curl`, and `get_stage8_ui_demo_handoff_bundle.sh`. No ingestion or network beyond DB + localhost HTTP.

## Stage 8 Design Branch And Post-Figma Production Status

Stage 8 preserves synchronized baselines:
- validated runtime preview / demo handoff checkpoint through `AI Task 061` (**implementation floor** — unchanged by Figma work)
- validated external near-complete core UI baseline under `docs/design/artifacts/task064/`
- certified IA baseline under `docs/design/artifacts/task065/` (post–`AI Task 073` / reopened `AI Task 066` **pass / GO**)
- certified visual-system baseline under `docs/design/artifacts/task074/` (`AI Task 075` **pass / GO for 076**)
- completed screen prompt baseline under `docs/design/prompts/figma_screen_prompts.md` and `docs/design/prompts/figma_screen_prompt_submission_checklist.md` (`AI Task 076`)
- workspace-registered screen evidence package under `docs/design/artifacts/task076/` (`AI Task 077` revision 2 pass / GO)
- **canonical import record:** `docs/design/approved_figma_artifact.md` (`AI Task 078`) — **`task076/`** remains the primary **artifact package**; design authority is explicit and separate from **runtime** JSON (`contextJSON` + contracts)
- **`AI Task 079` (complete):** post-Figma **plan refinement** — production UI slices **`080–083`** and validation commands: `docs/plans/implementation-plan.md` (see §Post-Figma roadmap)

**Design-sync track (062–079): complete.** Legacy files `067–072` remain superseded placeholders.

**Production UI track (`080–083`): complete.** **`080`** — shell + tokens; delivery smoke confirms `data-cv-preview-shell="080"`. **`081`** — **Overview** **`overview-surface`** from dashboard feed + project overview. **`082`** — **Visualization** **`viz-workspace`** (tree + graph + inspector) from visualization contracts only. **`083`** — **History** **`history-workspace`** (daily rollup + timeline + checks) from history bundle fields only; `render_profile` **`083_history_handoff_fidelity`**; demo handoff + delivery smokes assert all three production surface class roots on served HTML. **Stage 8 is now complete; no next numbered task is defined until the next stage / next implementation slice is decomposed.** Charter/workflow: `docs/design/figma_design_branch_charter.md`, `docs/design/figma_prompt_workflow.md`.

## Stage 9 — Secondary flows (Diff viewer contract)

`code/diff/get_diff_viewer_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer) and `-h` / `--help`. Invokes `get_latest_valid_snapshot_projection.sh`, `get_latest_snapshot_diff_summary.sh`, and `get_valid_snapshot_timeline_projection.sh` (Stage 4 interpretation scripts), and — when the diff summary reports a previous valid snapshot id — loads that snapshot row from `snapshots` (`is_valid = true`) for `previous_snapshot.projection`. Prints one JSON object: `project_id`, `generated_at`, `status` (`ok`), `comparison_ready` (true only when two newest valid snapshots exist), `latest_snapshot` / `previous_snapshot` (`snapshot_id`, `snapshot_timestamp`, `projection`; previous null-safe), `diff_summary` with the same top-level-key arrays as Task 016 (`added_*`, `removed_*`, `changed_*`), `viewer_context` (counts and UX-only hints, no invented metrics), and `consistency_checks` (alignment of ids across sources). With 0–1 valid snapshots, diff arrays are empty and `comparison_ready` is false; exit `0` unless CLI/DB failure. Depends on `jq`, `psql`, and executable interpretation scripts. No markdown or non-DB runtime inputs.

`code/diff/verify_stage9_diff_viewer_contracts.sh` is read-only: requires `--project-id <id>`, optional `--invalid-project-id <value>` (default `abc`), and `-h` / `--help`. Runs `get_diff_viewer_contract_bundle.sh --project-id <id>` (JSON shape, all `consistency_checks` true, `comparison_ready` matches `valid_snapshots_count >= 2`) plus negative exit checks (missing `--project-id` → `2`, invalid `--project-id` → `1`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Exit non-zero when `status` is `fail`. Depends on `jq` and the bundle script. No ingestion or network beyond DB connectivity.

**AI Task 085 — Diff viewer preview surface:** `render_ui_bootstrap_preview.sh` adds the **`diff-workspace`** HTML region and merged **`ui_sections.diff_viewer`** in the bootstrap payload; `prepare_ui_preview_launch.sh` surfaces **`diff_viewer_state`** in `preview_summary`; `get_stage8_ui_preview_readiness_report.sh` requires the diff section for **`investor_demo_ready`**; `verify_stage8_ui_preview_delivery.sh` and **`verify_stage8_ui_demo_handoff_bundle.sh`** assert **`data-section="diff"`** and **`class="diff-workspace"`** on served HTML. All diff-viewer display fields remain sourced **only** from `get_diff_viewer_contract_bundle.sh`. Run `bash code/diff/verify_stage9_diff_viewer_contracts.sh --project-id <id>` together with Stage 8 UI delivery/handoff smokes after changes.

## Stage 9 — Settings / profile contract

`code/settings/get_settings_profile_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer) and `-h` / `--help`. Composes **`get_project_overview_feed.sh`** (project identity + overview aggregates), **`get_project_import_status.sh`** (integration status + import log + raw snapshot counts), and **`get_latest_valid_snapshot_projection.sh`** (latest **valid** snapshot row). Prints one JSON object: `project_id`, `generated_at`, `status` (`ok`), `profile` (DB-backed identity, integration lines, snapshot context; **no** user accounts, preferences, or feature flags), `settings_surface_state` (readiness booleans + hint only; **`user_preferences_in_contract`** and **`writable_product_settings_supported`** are explicitly `false`), `data_sources` (three script paths + roles), and `consistency_checks` (`project_ids_aligned`, `overview_import_status_matches_integration_feed` — normalizes overview `latest_import_status` `null` to `never_imported` vs import feed — plus latest-valid timestamp alignment and valid-snapshot count vs projection presence). Unknown project → overview exits `4` (stderr); failed checks → stdout JSON then exit **`3`**. Safe when import never ran, when imports exist but **`total_valid_snapshots`** is `0`, or when valid snapshots exist. Depends on `jq`, `psql`, and executable child scripts. No markdown runtime input.

`code/settings/verify_stage9_settings_profile_contracts.sh` is read-only: requires `--project-id <id>`, optional `--invalid-project-id <value>` (default `abc`), and `-h` / `--help`. Runs `get_settings_profile_contract_bundle.sh` (full shape, all `consistency_checks` true, `profile.project_id` matches `--project-id`) and negative CLI checks (missing `--project-id` → `2`, invalid id → `1`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Exit non-zero when `status` is `fail`. Depends on `jq` and the bundle script.

**AI Task 088 — Settings/profile preview + secondary-flows gate:** `render_ui_bootstrap_preview.sh` adds **`settings-workspace`** and merged **`ui_sections.settings_profile`** (086 bundle only); readiness / investor path requires **`settings_profile_available`** and read-only contract flags in **`settings_surface_state`**. **`verify_stage9_secondary_flows_readiness_gate.sh`** (`code/ui/`) runs Stage 9 contracts plus Stage 8 preview checks with the same `--project-id`, `--port`, `--output-dir`, and **`--invalid-project-id`** (and **`--mode fast|full`**, default **`fast`** — **AI Task 090**). **`fast`** runs **one** readiness subprocess and derives delivery + handoff readiness from readiness fields (`verification.delivery_smoke`, `readiness_summary.investor_demo_ready`) with no duplicate heavy subprocesses; **`full`** preserves standalone delivery + handoff + readiness subprocess order for diagnostics. Output shape unchanged: `status`, `checks[]`, `failed_checks`, `generated_at`; non-zero exit when `status` is `fail`. Anti-hang guard: child subprocess timeout is controlled by env **`STAGE9_GATE_TIMEOUT_S`** (default `420`, minimum `30`).

## Stage 9 — Completion and transition readiness

`code/ui/get_stage9_completion_gate_report.sh` is read-only orchestration: requires **`--project-id <id>`** (non-negative integer), optional **`--mode fast|full`** (default **`fast`** — **AI Task 090**), optional **`--port`**, **`--output-dir`**, **`--invalid-project-id`**. **`fast`**: runs contract verifiers + **one** **`get_stage8_ui_preview_readiness_report.sh`**, derives delivery from **`verification.delivery_smoke`**, derives handoff readiness from **`readiness_summary.investor_demo_ready`**, and inlines secondary-flows gate criteria without redundant subprocesses. **`full`**: six subprocesses as in **AI Task 089** (standalone delivery, handoff, readiness, and **`verify_stage9_secondary_flows_readiness_gate --mode full`**). Top-level JSON contract unchanged. **Exit `0`** / **`3`** semantics unchanged. Invalid **`--mode`**: stderr, exit **`2`**. Anti-hang guard: child subprocess timeout via env **`STAGE9_GATE_TIMEOUT_S`** (default `420`, minimum `30`).

`code/ui/verify_stage9_completion_gate.sh` is read-only: forwards **`--mode fast|full`** (default **`fast`**) to **`get_stage9_completion_gate_report.sh`**. Validates report shape and readiness-for-transition fields; negatives include **invalid `--mode`** on the report (**`2`**). It also uses the same anti-hang child timeout env **`STAGE9_GATE_TIMEOUT_S`** (default `420`, minimum `30`). **AI Tasks 089–090** — Stage 9 closure gate + fast smoke defaults.
