# Data Layer (Stage 2)

This folder stores concrete data-layer implementation artifacts for ContextViewer.

Schema artifacts:
- `projects` / `snapshots` (AI Task 004+)
- `insert_snapshot_dedup(...)` — idempotent insert with filename and content-hash deduplication (AI Task 007)
- `snapshot_import_logs` / `insert_snapshot_import_log(...)` — per-project import/refresh status and messages (AI Task 008)

Out of scope for this folder:
- ingestion orchestration
- runtime snapshot selection
- dashboard / reporting APIs

## Stage 3 Ingestion

Read-only GitHub listing for `contextJSON` lives in `code/ingestion/github_contextjson_connector.sh` (GitHub Contents API → normalized JSON array of `*.json` files with `name`, `path`, `size`, `sha`, `download_url`). No database or import pipeline in that script.

`code/ingestion/contextjson_file_scanner.sh` reads that array from stdin, splits entries into `valid_files` / `invalid_files` using the `json_YYYY-MM-DD_HH-MM-SS.json` filename rule, and sets `latest_valid_file` to the row with the maximum extracted timestamp (no DB or runtime selection).

`code/ingestion/import_contextjson_pipeline.sh` chains connector → scanner, downloads each valid `download_url`, SHA-256-hashes the raw body, calls `insert_snapshot_dedup` / `insert_snapshot_import_log`, and prints one JSON summary (stdout). Requires `PROJECT_ID`, GitHub env vars, and `psql` connectivity.

`code/ingestion/refresh_contextjson_ingestion.sh` is the only supported entrypoint for running that pipeline in MVP: pass `manual_refresh` or `project_open`, get one JSON object with `trigger_source`, UTC `started_at` / `finished_at`, and nested `pipeline` (the pipeline summary). Invalid triggers do not run the importer; no cron/daemon logic.

`code/ingestion/get_project_import_status.sh` is read-only: given a numeric `project_id`, it queries `snapshot_import_logs` (latest row) and `snapshots` (count + max filename-derived `timestamp`) and prints one JSON object (`integration_status`, `latest_import_log`, etc.). No pipeline, network, or background work.

`code/ingestion/verify_stage3_ingestion_contracts.sh` runs contract smoke checks (connector → scanner → pipeline → refresh → import status) when `GITHUB_*`, `PROJECT_ID`, and `psql` connectivity are available, and prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Use `--help` for prerequisites; no daemons or UI.

## Stage 4 Interpretation

`code/interpretation/get_latest_valid_snapshot_projection.sh` is read-only: given a numeric `project_id`, it returns the newest `snapshots` row with `is_valid = true` ordered by filename-derived `timestamp` DESC (then `id` DESC), and prints one JSON object with `project_id`, `snapshot_id`, `snapshot_timestamp`, and `projection` (the row’s `raw_json`, or all nulls if none). No ingestion or network.

`code/interpretation/get_latest_snapshot_diff_summary.sh` is read-only: compares the two latest valid snapshots’ `raw_json` at **top-level keys only** (`added_*`, `removed_*`, `changed_*` arrays). With 0–1 valid snapshots, ids and diff arrays are empty where required; exit 0. No ingestion or network.

`code/interpretation/get_latest_changes_since_previous_projection.sh` is read-only: loads latest (+ previous id) valid snapshots (`timestamp` DESC, `id` DESC) and returns `changes_since_previous` and `changes_count` from the **latest** `raw_json` when that key is a JSON array; otherwise empty array and count 0. No ingestion or network.

`code/interpretation/get_latest_roadmap_progress_projection.sh` is read-only: latest valid snapshot only (`timestamp` DESC, `id` DESC) and returns `roadmap` (array) plus `progress` `{ implemented, in_progress, next }` (each array) with empty safe fallbacks if keys or types are wrong. No ingestion or network.

`code/interpretation/get_latest_current_status_projection.sh` is read-only: same latest valid snapshot selection and returns one JSON object with `project_id`, `latest_snapshot_id`, and `current_status` combining `progress` arrays (`implemented`, `in_progress`, `next`) with `changes_since_previous` (array from `raw_json` when valid). Missing snapshots or bad types fall back to empty arrays; no ingestion or network.

`code/interpretation/get_valid_snapshot_timeline_projection.sh` is read-only: lists all `snapshots` with `is_valid = true` for a numeric `project_id` as `timeline` (`snapshot_id`, `file_name`, `snapshot_timestamp`, `import_time`), ordered by `timestamp` DESC then `id` DESC, plus `total_valid_snapshots`. Empty project yields `0` and `[]`. No ingestion or network.

`code/interpretation/get_interpretation_bundle_projection.sh` is read-only: given a numeric `project_id`, runs the six Stage 4 interpretation scripts above (latest snapshot, diff summary, changes projection, roadmap/progress, current status, valid snapshot timeline) and prints one JSON object with `project_id`, `bundle_generated_at` (UTC), and each script’s full output under `latest_snapshot`, `diff_summary`, `changes_projection`, `roadmap_progress`, `current_status`, and `timeline`. No ingestion, network, or extra DB logic beyond those scripts.

`code/interpretation/get_dashboard_feed_projection.sh` is read-only: calls `get_interpretation_bundle_projection.sh` and prints a dashboard-oriented object (`project_id`, `generated_at`, `overview` with snapshot time, counts, and diff/changes metrics, plus `roadmap`, `progress` `{ implemented, in_progress, next }`, and `timeline` as the snapshot row array). Safe fallbacks when sections are empty or malformed. No ingestion or network.

`code/interpretation/verify_stage4_interpretation_contracts.sh` runs JSON contract smoke checks for the eight Stage 4 interpretation entrypoints (latest snapshot, diff summary, changes projection, roadmap/progress, current status, valid snapshot timeline, interpretation bundle, dashboard feed) given a numeric `project_id`, and prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Requires `jq` and working `psql` like the scripts under test; invalid `project_id` yields `fail` with `failed_checks > 0`. Exit code is non-zero when `status` is `fail`. No ingestion or network beyond DB connectivity.

## Stage 5 Dashboard Core

`code/dashboard/get_project_list_overview_feed.sh` is read-only: no arguments (optional `--help`); queries `projects`, latest `snapshot_import_logs` per project, and valid `snapshots` aggregates; prints one JSON object (`generated_at`, `total_projects`, `projects[]` with metadata, `latest_import_status` / `latest_import_time`, `latest_valid_snapshot_timestamp`, `total_valid_snapshots`). Sorted by `created_at` DESC then `project_id` DESC. Empty database yields `0` and `[]`. No ingestion or network.

`code/dashboard/get_project_overview_feed.sh` is read-only: given a numeric `project_id`, returns one JSON overview for that row (same import/snapshot fields as the list feed entry, plus `overview_generated_at`). Unknown id yields a clear stderr error and non-zero exit. No ingestion or network.

`code/dashboard/get_dashboard_home_feed.sh` is read-only: calls `get_project_list_overview_feed.sh` and prints `generated_at`, `summary` (`total_projects`, `projects_with_import_status`, `projects_with_valid_snapshots`), `projects`, and `selected_project_overview` (null unless `--project-id <id>` is set, then `get_project_overview_feed.sh`). Invalid CLI or unknown project id exits non-zero with stderr. No ingestion or network.

`code/dashboard/get_project_dashboard_feed.sh` is read-only: given a numeric `project_id`, calls `get_project_overview_feed.sh` (project must exist) then `get_dashboard_feed_projection.sh`, and prints `generated_at`, `project_overview`, and `dashboard_feed`. Invalid/missing id or child failure yields stderr + non-zero exit. No ingestion or network.

`code/dashboard/verify_stage5_dashboard_contracts.sh` runs JSON contract smoke checks for the four Stage 5 dashboard entrypoints (list overview, project overview, home feed with `--project-id`, project dashboard feed) plus negative exit checks for invalid `--invalid-project-id` (default `abc`). Requires `--project-id <id>` for positive checks and prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Exit code is non-zero when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/dashboard/get_dashboard_api_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer); runs the four dashboard scripts above and prints one JSON object (`generated_at`, `contracts` with `project_list_overview`, `project_overview`, `dashboard_home`, `project_dashboard`, and `consistency_checks` with `project_id_match` and `project_present_in_list`). Invalid/missing project or child failure yields stderr + non-zero exit. No ingestion or network.

## Stage 6 Visualization

`code/visualization/get_architecture_tree_feed.sh` is read-only: given a numeric `project_id`, ensures the project exists, then reads the latest valid snapshot’s `raw_json.architecture_tree` and prints `project_id`, `generated_at`, `snapshot_id` (or null), and a flattened `tree` (`path`, `type` as `file` or `directory`, `label`). Missing `architecture_tree` or no valid snapshot yields `tree` `[]` and `snapshot_id` null with exit 0. Unknown project exits non-zero. No ingestion or network.

`code/visualization/get_architecture_graph_feed.sh` is read-only: same snapshot selection as the tree feed; reads `raw_json.architecture_graph` and prints `project_id`, `generated_at`, `snapshot_id` (or null), and `graph` with normalized `nodes` (`id`, `label`, `type`) and `edges` (`source`, `target`, `relation`; accepts legacy `from`/`to` in source JSON). Empty or missing graph data yields empty arrays with exit 0. Unknown project exits non-zero. No ingestion or network.

`code/visualization/verify_stage6_visualization_contracts.sh` runs JSON contract smoke checks for `get_architecture_tree_feed.sh` and `get_architecture_graph_feed.sh` (positive shape + negative invalid id), given `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_bundle_feed.sh` is read-only: one positional `project_id` (non-negative integer) and optional `-h`/`--help`. Runs `get_architecture_tree_feed.sh` and `get_architecture_graph_feed.sh` and prints one JSON object with `project_id`, `generated_at`, full `architecture_tree` and `architecture_graph` child outputs, and `consistency_checks` (`project_id_match`, `snapshot_id_match` between the two payloads). Invalid id or missing project propagates child stderr and non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_runtime_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer) and `-h`/`--help`. Calls `code/dashboard/get_project_overview_feed.sh` and `get_visualization_bundle_feed.sh`, and prints one lightweight JSON object (`generated_at`, `project_id`, `project_overview` subset with `project_id`, `name`, `latest_valid_snapshot_timestamp`, `total_valid_snapshots`, `visualization` subset with `snapshot_id`, `tree`, `graph.nodes`, `graph.edges`, and `consistency_checks` with `project_id_match`, `snapshot_id_match`). No smoke or contract bundles. Missing/invalid `--project-id` or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_runtime_contracts.sh` runs JSON contract smoke checks for `get_visualization_runtime_feed.sh`, `get_project_visualization_feed.sh`, and `get_visualization_home_feed.sh --project-id`, plus negative exit checks for invalid/missing `--project-id` on `get_visualization_runtime_feed.sh`. Requires `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_api_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`) for the verify step, and `-h`/`--help`. Runs `get_architecture_tree_feed.sh`, `get_architecture_graph_feed.sh`, `get_visualization_bundle_feed.sh`, and `verify_stage6_visualization_contracts.sh` and prints one JSON object (`generated_at`, `contracts` with `architecture_tree`, `architecture_graph`, `visualization_bundle`, `visualization_contract_smoke`, and `consistency_checks` with `project_id_match`, `snapshot_id_match`, `smoke_status_pass`). Missing/invalid `--project-id` or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/get_project_visualization_feed.sh` is read-only: one positional `project_id` (non-negative integer) and optional `-h`/`--help`. Calls `code/dashboard/get_project_overview_feed.sh` and `get_visualization_api_contract_bundle.sh --project-id <id>`, and prints one JSON object (`generated_at`, `project_overview`, `visualization`, `consistency_checks` with `project_id_match`, `snapshot_alignment`, `smoke_status_pass`). Invalid id, missing project, or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_home_feed.sh` is read-only: no arguments or optional `--project-id <id>` and `-h`/`--help`. Calls `code/dashboard/get_project_list_overview_feed.sh` and, when `--project-id` is set, `get_project_visualization_feed.sh` for that id. Prints one JSON object (`generated_at`, `summary` with `total_projects`, `projects_with_import_status`, `projects_with_valid_snapshots`, `projects_with_visualization_data`, `projects`, `selected_project_visualization` object or null). Without `--project-id`, `selected_project_visualization` is null. Invalid/missing project or child failure yields stderr + non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_api_contracts.sh` runs JSON contract smoke checks for the five Stage 6 visualization/API entrypoints (`get_architecture_tree_feed.sh`, `get_architecture_graph_feed.sh`, `get_visualization_bundle_feed.sh`, `verify_stage6_visualization_contracts.sh`, `get_visualization_api_contract_bundle.sh`) plus negative exit checks for invalid `--invalid-project-id` on `get_visualization_bundle_feed.sh` and `get_visualization_api_contract_bundle.sh`, given `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_home_contracts.sh` runs JSON contract smoke checks for `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh` (base and with `--project-id`), and `get_visualization_api_contract_bundle.sh`, plus negative exit checks for invalid ids on `get_project_visualization_feed.sh` and `get_visualization_home_feed.sh --project-id`. Requires `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_visualization_workspace_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`) for the API bundle and smoke suites, and `-h`/`--help`. Aggregates `get_visualization_home_feed.sh` (base and with `--project-id`), `get_project_visualization_feed.sh`, `get_visualization_api_contract_bundle.sh`, `verify_stage6_visualization_contracts.sh`, `verify_stage6_visualization_api_contracts.sh`, and `verify_stage6_visualization_home_contracts.sh`, and prints one JSON object (`generated_at`, `contracts` with `visualization_home_base`, `visualization_home_selected`, `project_visualization`, `visualization_api_bundle`, `visualization_smoke`, `visualization_api_smoke`, `visualization_home_smoke`, and `consistency_checks` with `project_id_match`, `snapshot_id_match`, `all_smokes_pass`). Missing/invalid `--project-id`, unknown project, strict child failure, or failed consistency checks yields stderr and/or non-zero exit. No ingestion or network beyond DB connectivity.

`code/visualization/verify_stage6_visualization_workspace_contracts.sh` runs JSON contract smoke checks for `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh` (base and with `--project-id`), `get_visualization_api_contract_bundle.sh`, and `get_visualization_workspace_contract_bundle.sh`, plus negative exit checks for invalid ids on `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh --project-id`, and `get_visualization_workspace_contract_bundle.sh --project-id`. Requires `--project-id <id>` and optional `--invalid-project-id` (default `abc`). Prints one JSON report (`status`, `checks[]`, `failed_checks`, `generated_at`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/benchmark_stage6_visualization_latency.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--iterations <n>` (integer >= 1, default `1`), and `-h`/`--help`. Times wall-clock latency (milliseconds) for `get_architecture_tree_feed.sh`, `get_architecture_graph_feed.sh`, `get_visualization_bundle_feed.sh`, `get_visualization_api_contract_bundle.sh`, `get_project_visualization_feed.sh`, `get_visualization_home_feed.sh --project-id`, and `get_visualization_workspace_contract_bundle.sh --project-id`, and prints one JSON report (`status`, `project_id`, `iterations`, `generated_at`, `benchmarks[]` with `name`, `iterations`, `durations_ms`, `avg_ms`, `max_ms`, `status`, `error`, and `summary` with `total_checks`, `failed_checks`, `slowest_check_name`, `slowest_check_max_ms`). Invalid CLI, missing scripts, or any failed benchmark yields stderr and/or non-zero exit. Uses `python3` or `perl` for millisecond timing. No ingestion or network beyond DB connectivity.

`code/visualization/check_stage6_visualization_latency_guardrails.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--iterations <n>` (integer >= 1, default `1`), optional `--max-ms <value>` (integer >= 1, default `120000`), and `-h`/`--help`. Runs `benchmark_stage6_visualization_latency.sh` with the same `--project-id` and `--iterations`, then fails if any benchmark entry has `status != "ok"` or `max_ms > threshold_max_ms`. Prints one JSON (`status`, `project_id`, `iterations`, `threshold_max_ms`, `generated_at`, `benchmark` full output, `violations` with `name`, `max_ms`, `reason`, and `summary` with `checks_total`, `violations_total`, `slowest_check_name`, `slowest_check_max_ms`). Non-zero exit when `status` is `fail`. No ingestion or network beyond DB connectivity.

`code/visualization/get_stage6_visualization_readiness_report.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--iterations <n>` (default `1`), `--max-ms <value>` (default `120000`), `--invalid-project-id <value>` (default `abc`), and `-h`/`--help`. Runs `check_stage6_visualization_latency_guardrails.sh`, `verify_stage6_visualization_runtime_contracts.sh`, and `verify_stage6_visualization_workspace_contracts.sh`, and prints one JSON object (`generated_at`, `project_id`, `ready_for_runtime`, `readiness_checks` with `latency_guardrails_pass`, `runtime_contracts_pass`, `workspace_contracts_pass`, and `details` with full child payloads). `ready_for_runtime` is true only when all three child `status` values are `pass`. Exit 0 when `ready_for_runtime` is true; non-zero otherwise. Invalid CLI yields stderr without JSON. No ingestion or network beyond DB connectivity.

`code/visualization/get_stage6_completion_gate_report.sh` is read-only: same CLI as `get_stage6_visualization_readiness_report.sh`. Calls `get_stage6_visualization_readiness_report.sh` with the same arguments and prints one JSON object (`generated_at`, `project_id`, `stage` set to `Stage 6`, `can_transition_to_stage7`, `gate_checks` with `runtime_ready`, `latency_within_threshold`, `runtime_contracts_pass`, `workspace_contracts_pass`, `blocking_reasons` string array, and `readiness_report` full payload). `can_transition_to_stage7` is true only when all gate checks are true. Exit 0 when `can_transition_to_stage7` is true; non-zero otherwise. No ingestion or network beyond DB connectivity.

## Stage 7 History

`code/history/get_project_history_daily_rollup_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional inclusive UTC day bounds `--from` / `--to` (`YYYY-MM-DD`), and `-h` / `--help`. Queries `snapshots` with `is_valid = true` for that project, groups by UTC calendar day derived from the filename-derived `timestamp` column (same notion as Stage 4 timeline). Prints one JSON object: `project_id`, `generated_at` (UTC), `range` `{ from, to }` (each string or `null` when the flag is omitted), `summary` with `days_with_activity`, `total_valid_snapshots` (equals the sum of `days[].valid_snapshots_count`), and `latest_snapshot_timestamp` (string or `null`), plus `days[]` sorted by `date` descending with `date`, `valid_snapshots_count`, `latest_snapshot_timestamp`, and `snapshot_ids` (numeric ids, newest snapshot first within each day). Invalid `--project-id` format → stderr, exit `1`; project not found → stderr, exit `4`; invalid dates or `from > to` → stderr, exit `1`; database/query failure → stderr, exit `3`. Depends on `psql`, `jq`, and `python3` for calendar date validation. No ingestion or network beyond DB connectivity.

`code/history/get_project_history_timeline_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional inclusive UTC day bounds `--from` / `--to` (`YYYY-MM-DD`), optional `--limit <n>` (integer ≥ 1, default `200`), and `-h` / `--help`. Queries `snapshots` with `is_valid = true` for that project, ordered by filename-derived `timestamp` DESC then `id` DESC, capped at `--limit`. Prints one JSON object: `project_id`, `generated_at` (UTC), `range` `{ from, to, limit }` (`from` / `to` string or `null` when omitted; `limit` is the requested cap), `summary` with `total_returned` (equals `timeline | length`), `latest_snapshot_timestamp`, and `oldest_snapshot_timestamp` (each among returned rows, or `null` when empty), and `timeline[]` with `snapshot_id`, `file_name`, `snapshot_timestamp`, `import_time` (UTC string or `null`), and `day` (`YYYY-MM-DD`, same UTC-day notion as the daily rollup). Invalid `--project-id` format → stderr, exit `1`; project not found → stderr, exit `4`; invalid dates, `from > to`, or invalid `--limit` → stderr, exit `1`; database/query failure → stderr, exit `3`. Depends on `psql`, `jq`, and `python3` for calendar date validation. No ingestion or network beyond DB connectivity.

`code/history/get_project_history_bundle_feed.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--from` / `--to` (`YYYY-MM-DD`), optional `--limit <n>` (integer ≥ 1, default `200`; passed to the timeline feed only), and `-h` / `--help`. Invokes `get_project_history_daily_rollup_feed.sh` and `get_project_history_timeline_feed.sh` with the same `--project-id` and date bounds, and prints one JSON object: `project_id`, `generated_at` (UTC for the bundle), `range` (copied from the timeline payload: `{ from, to, limit }`), `history` with full `daily` and `timeline` child outputs, and `consistency_checks` (`project_id_match`, `range_match` on `from`/`to`, `timeline_count_consistent`, `latest_timestamp_aligned`). Invalid CLI → stderr, non-zero exit; missing project or child failure → child stderr and exit code propagated; malformed child JSON or failed internal consistency checks → stderr, exit `3`. Depends on `jq` and executable child scripts. No ingestion or network beyond DB connectivity.

`code/history/verify_stage7_history_contracts.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc` for negative exit checks), and `-h` / `--help`. Runs JSON contract smoke checks for `get_project_history_daily_rollup_feed.sh`, `get_project_history_timeline_feed.sh`, and `get_project_history_bundle_feed.sh` (positive: exit `0` and shape validation; bundle must have all `consistency_checks` true), plus three negative checks expecting exit `1` when each script is invoked with invalid `--project-id`. Prints one JSON object: `status` (`pass` or `fail`), `checks` (`{ name, status, details }` per check), `failed_checks`, `generated_at` (UTC). Invalid top-level `--project-id` (not a non-negative integer) → stdout only: `status` `fail`, `failed_checks` `1`, check `project_id`, exit `1`. Missing `--project-id` or unknown args → stderr, non-zero exit. Exit `0` only when `status` is `pass`. Depends on `jq` and the three history scripts. No ingestion or network beyond DB connectivity.

`code/history/get_history_home_feed.sh` is read-only: no arguments (base payload) or optional `--project-id <id>` (non-negative integer) and `-h` / `--help`. Calls `code/dashboard/get_project_list_overview_feed.sh`, and when `--project-id` is set calls `get_project_history_bundle_feed.sh --project-id <id>`. Prints one JSON object: `generated_at` (UTC), `summary` with `total_projects`, `projects_with_valid_snapshots`, `projects_with_history_data` (count where `latest_valid_snapshot_timestamp` is not null), `projects` (same array as the list feed), `selected_project_history` (full bundle JSON or `null` when no project selected), and `consistency_checks` (`summary_total_matches_projects_length`, `selected_project_id_match`, `selected_history_consistent`). Invalid `--project-id` format → stderr, exit `1`; unknown project or bundle failure → child stderr and exit code propagated; list feed failure or invalid JSON → stderr, exit `3`; failed root `consistency_checks` after successful children → stderr, exit `3`. Depends on `jq` and child scripts. No ingestion or network beyond DB connectivity.

`code/history/get_history_workspace_contract_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`, forwarded to `verify_stage7_history_contracts.sh`), and `-h` / `--help`. Aggregates `get_history_home_feed.sh` (base), `get_history_home_feed.sh --project-id <id>`, `get_project_history_bundle_feed.sh --project-id <id>`, and `verify_stage7_history_contracts.sh --project-id <id> --invalid-project-id <value>`. Prints one JSON object: `generated_at` (UTC), `contracts` with `history_home_base`, `history_home_selected`, `project_history_bundle`, `history_api_smoke`, and `consistency_checks` (`project_id_match`, `selected_bundle_match`, `history_smoke_pass`). Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; unknown project or non-smoke child failure → propagated exit; invalid JSON from a required child → stderr, exit `3`; any `consistency_checks` false after successful JSON assembly → stdout JSON then exit `3`. The smoke suite’s JSON is always captured (even when its exit status is non-zero); `history_smoke_pass` requires `history_api_smoke.status == "pass"`. Depends on `jq` and executable child scripts. No ingestion or network beyond DB connectivity.

## Stage 8 Polish

`code/ui/get_ui_bootstrap_bundle.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`, passed to both workspace bundles), and `-h` / `--help`. Aggregates `code/dashboard/get_project_dashboard_feed.sh` with a **positional** project id (same as Stage 5 — not `--project-id`), `get_visualization_workspace_contract_bundle.sh --project-id <id> --invalid-project-id <value>`, and `get_history_workspace_contract_bundle.sh --project-id <id> --invalid-project-id <value>`. Prints one JSON object: `generated_at` (UTC), `project_id`, `ui_sections` with `overview` (full dashboard feed: `project_overview` + `dashboard_feed`), `visualization_workspace` (full Stage 6 workspace bundle), `history_workspace` (full Stage 7 workspace bundle), and `consistency_checks` (`project_id_match`, `overview_present`, `visualization_consistent`, `history_consistent`). Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; unknown project or child failure → propagated exit; invalid JSON from any child → stderr, exit `3`; any root `consistency_checks` false → stdout JSON then exit `3`. Depends on `jq` and executable child scripts. No ingestion or network beyond DB connectivity.

`code/ui/verify_stage8_ui_bootstrap_contracts.sh` is read-only: requires `--project-id <id>` (non-negative integer), optional `--invalid-project-id <value>` (default `abc`, passed to `get_ui_bootstrap_bundle.sh` for the positive run and for the negative invalid-id check), and `-h` / `--help`. Runs `get_ui_bootstrap_bundle.sh --project-id <id> --invalid-project-id <value>` and validates exit `0`, JSON shape (`project_id`, `generated_at`, `ui_sections.overview` / `visualization_workspace` / `history_workspace`, boolean `consistency_checks.*`), and that all bootstrap consistency flags are `true`; also runs negative checks on the bootstrap script (no args → expected exit `2`; `--project-id <invalid>` → expected exit `1`). Prints one JSON object: `status` (`pass` or `fail`), `checks` (`{ name, status, details }` per check), `failed_checks`, `generated_at` (UTC). Missing `--project-id` on this script or unknown args → stderr, exit `2`. Invalid top-level `--project-id` (not a non-negative integer) → stdout only: `status` `fail`, `failed_checks` `1`, check `project_id`, exit `1`. Exit `0` only when `status` is `pass`. Depends on `jq` and `get_ui_bootstrap_bundle.sh`. No ingestion or network beyond DB connectivity.

`code/ui/render_ui_bootstrap_preview.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), `--output <path>` (destination HTML file), optional `--invalid-project-id <value>` (default `abc`, passed to `get_ui_bootstrap_bundle.sh`), and `-h` / `--help`. Calls `get_ui_bootstrap_bundle.sh`, then writes one self-contained HTML file (inline CSS only; `data-section` markers `overview`, `visualization`, `history`; embedded bootstrap JSON in `<script type="application/json" id="ui-bootstrap-payload">` with `</script>`-safe escaping). Prints one JSON object to stdout: `project_id`, `generated_at` (UTC), `output_file`, `sections_rendered` (`overview`, `visualization`, `history`), `source_consistency_checks` (from the bootstrap payload). Missing `--project-id` or `--output` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; bootstrap/child failure → propagated exit; invalid bootstrap JSON or failed write → stderr, exit `3`. Depends on `jq`, `python3` (HTML escaping), and `get_ui_bootstrap_bundle.sh`. Creates/overwrites only the `--output` file (and `mkdir -p` its parent directory). No ingestion or network beyond DB connectivity.

`code/ui/prepare_ui_preview_launch.sh` is read-only against source data: requires `--project-id <id>` (non-negative integer), optional `--output-dir <path>` (default `/tmp/contextviewer_ui_preview`), optional `--invalid-project-id <value>` (default `abc`, passed to `render_ui_bootstrap_preview.sh`), and `-h` / `--help`. Ensures `--output-dir` exists (`mkdir -p`), then calls `render_ui_bootstrap_preview.sh` with `--output <output-dir>/contextviewer_ui_preview_<project-id>.html`. Prints one JSON object: `project_id`, `generated_at`, `output_dir` (absolute), `output_file` (absolute), `open_command` (plain string `open <quoted-absolute-path>` for macOS), `preview_summary` with `sections_rendered` and `source_consistency_checks` (from the render output). Missing `--project-id` or unknown args → stderr, exit `2`; invalid `--project-id` format → stderr, exit `1`; render failure → propagated exit; `mkdir` / directory resolution failure or invalid child JSON → stderr, exit `3`. Depends on `jq` and `render_ui_bootstrap_preview.sh`. Writes only the generated HTML under `--output-dir`. No ingestion or network beyond DB connectivity.
