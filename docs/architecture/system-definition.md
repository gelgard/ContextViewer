# System Definition — ContextViewer

Latest valid contextJSON snapshot = runtime truth.

Full system definition including:
- dashboard behavior
- ingestion rules
- stage plan
- execution model

This file is the single source of truth.
